{
  "name": "@blast/web",
  "version": "0.1.0",
  "private": true,
  "description": "The blast demo surface: a brief rendered by the same engine the agent runs.",
  "license": "Apache-2.0",
  "type": "module",
  "scripts": {
    "build": "next build --webpack",
    "dev": "next dev --webpack --port 3100",
    "start": "next start",
    "test:unit": "vitest run --config vitest.config.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@blast-fixtures/storefront": "workspace:*",
    "@blast/adapters": "workspace:*",
    "@blast/brief": "workspace:*",
    "@blast/core": "workspace:*",
    "eve": "0.64.1",
    "next": "16.3.6",
    "react": "19.3.0",
    "react-dom": "19.3.0",
    "server-only": "0.0.1"
  },
  "devDependencies": {
    "@types/react": "19.3.0",
    "@types/react-dom": "19.3.0",
    "typescript": "catalog:",
    "vitest": "catalog:"
  },
  "engines": {
    "node": ">=24"
  }
}
